# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# venv
.venv/
venv/
env/

# local config / secrets (never commit)
~/.karvyloop/
config.yaml
*.key
.env

# IDE / caches
.idea/
.vscode/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.swp

# 本地内部草稿/设计(绝不入公开仓)
_local/

# 模型产物兜底:压测/演示若往包内样例区写临时文件,绝不让它误入 git
# (workspace 已在测试里覆盖到 tmp;此行是第二道防线,产物命名无常按前缀兜)
karvyloop/sample_data/_*

# 测试产物(Playwright 截图等):按需生成给人工看,不入库(随每次跑变、占体积)
tests/_artifacts/
karvyloop/console/frontend/scripts/e2e_vectors.json

# 第三方设计 skill(本地工具,不入公开仓)
.agents/
.claude/skills/
